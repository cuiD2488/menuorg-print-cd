# 🔄 自动更新功能使用指南

## 概述

MenuorgPrint 现在支持自动更新功能，让您无需手动卸载重装就能升级到最新版本。系统会自动检查、下载并安装更新，确保您始终使用最新功能。

## ✨ 主要功能

### 1. 自动检查更新
- **启动时检查**：应用启动时自动检查是否有新版本
- **定时检查**：后台定期检查更新（可配置间隔）
- **手动检查**：随时点击按钮主动检查更新

### 2. 智能下载更新
- **自动下载**：发现更新后自动开始下载（可关闭）
- **断点续传**：支持网络中断后继续下载
- **进度显示**：实时显示下载进度和速度

### 3. 安全安装更新
- **用户确认**：安装前征求用户同意
- **数据保护**：安装过程中保护用户数据和配置
- **自动重启**：安装完成后自动重启应用

### 4. 多种通知方式
- **系统托盘**：从托盘菜单检查更新
- **应用内UI**：专门的更新管理界面
- **通知提示**：发现更新时自动弹出通知

## 🚀 快速开始

### 1. 检查更新
- **方法一**：点击应用内的"检查更新"按钮
- **方法二**：右键点击系统托盘图标 → "检查更新"
- **方法三**：等待自动检查（默认每4小时一次）

### 2. 下载更新
- 发现更新后，系统会询问是否下载
- 可以选择立即下载或稍后下载
- 下载过程中可以正常使用应用

### 3. 安装更新
- 下载完成后，点击"安装更新"按钮
- 确认安装后，应用会自动重启
- 重启后即可使用新版本

## ⚙️ 配置选项

### 更新设置
在应用设置页面的"应用更新"区域，您可以配置：

1. **启用自动检查更新**
   - 开启/关闭自动检查功能
   - 关闭后只能手动检查

2. **自动下载更新**
   - 开启：发现更新后自动下载
   - 关闭：需要手动点击下载

3. **检查间隔**
   - 1小时：适合开发测试环境
   - 4小时：默认设置，平衡及时性和性能
   - 12小时：减少检查频率
   - 24小时：每天检查一次

### 高级配置
如需更多配置选项，可编辑用户数据目录下的 `updater-config.json`：

```json
{
  "autoCheckInterval": 4,
  "autoDownload": true,
  "silentInstall": false,
  "checkOnStartup": true,
  "allowPrerelease": false
}
```

**配置说明**：
- `autoCheckInterval`：自动检查间隔（小时）
- `autoDownload`：是否自动下载更新
- `silentInstall`：是否静默安装（不推荐）
- `checkOnStartup`：启动时是否检查更新
- `allowPrerelease`：是否允许预发布版本

## 📱 使用界面

### 状态指示器
- 🔵 **就绪**：系统正常，无更新
- 🟡 **检查中**：正在检查更新
- 🟢 **有更新可用**：发现新版本
- 🔵 **下载中**：正在下载更新
- 🟦 **更新已就绪**：可以安装更新
- 🔴 **检查失败**：更新检查出错

### 按钮说明
- **检查更新**：手动触发更新检查
- **下载更新**：开始下载可用更新
- **安装更新**：安装已下载的更新

### 进度显示
下载过程中会显示：
- 下载进度百分比
- 下载速度
- 已下载/总大小

## 🔧 发布新版本

### 开发者指南

1. **更新版本号**
   ```bash
   # 修改 package.json 中的版本号
   "version": "1.1.0"
   ```

2. **构建和发布**
   ```bash
   # 构建并发布到GitHub Releases
   npm run release
   
   # 或者只构建不发布
   npm run build
   ```

3. **GitHub配置**
   - 确保在 `package.json` 中配置正确的 GitHub 仓库信息
   - 设置 GitHub Token 用于自动发布
   - 发布的文件会自动上传到 GitHub Releases

### 发布配置
在 `package.json` 的 `build.publish` 中配置发布源：

```json
{
  "publish": [
    {
      "provider": "github",
      "owner": "your-github-username", 
      "repo": "win7-print",
      "private": false
    }
  ]
}
```

**注意**：请将 `your-github-username` 替换为实际的 GitHub 用户名。

## 🛠️ 故障排除

### 常见问题

1. **检查更新失败**
   - 检查网络连接
   - 确认 GitHub 可访问
   - 查看控制台错误信息

2. **下载速度慢**
   - 网络环境影响
   - 可以暂停后稍后重试
   - 考虑使用网络加速工具

3. **安装失败**
   - 确保应用有足够权限
   - 关闭杀毒软件临时保护
   - 手动下载安装包重新安装

4. **更新后配置丢失**
   - 系统会自动备份配置
   - 检查用户数据目录
   - 重新导入配置文件

### 调试信息
查看更新日志：
- 打开开发者工具（F12）
- 查看控制台中的 `[AUTO-UPDATER]` 相关日志
- 检查用户数据目录下的日志文件

### 手动更新
如果自动更新失败，可以：
1. 从 GitHub Releases 页面下载最新版本
2. 备份当前配置和数据
3. 卸载旧版本
4. 安装新版本
5. 恢复配置和数据

## 📋 更新历史

系统会自动记录更新历史，包括：
- 更新版本和时间
- 更新内容说明
- 文件大小等信息

历史记录保存在用户数据目录的 `update-history.json` 文件中。

## 🔒 安全说明

### 更新安全
- 所有更新文件都经过数字签名验证
- 只从官方源下载更新
- 安装前会验证文件完整性

### 权限要求
- 更新过程可能需要管理员权限
- 系统会在需要时自动请求权限
- 用户可以选择拒绝或同意

### 数据保护
- 更新不会影响用户数据和配置
- 系统会自动备份重要配置
- 支持回滚到上一版本

## 📞 技术支持

如果您在使用自动更新功能时遇到问题：

1. 查看本指南的故障排除部分
2. 检查应用日志和错误信息  
3. 联系技术支持团队
4. 访问项目 GitHub 页面提交 Issue

---

**版本**: 1.0.0
**更新时间**: 2024年12月
**适用于**: MenuorgPrint v1.0.0+ 