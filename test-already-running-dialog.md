# 程序已运行对话框功能测试指南

## 功能概述

当用户尝试启动已经在运行的MenuorgPrint应用时，系统会显示一个友好的对话框，提示用户程序已经在运行，并提供相应的操作选项。

## 🎯 功能特性

### 1. **智能检测**
- ✅ 自动检测是否已有实例在运行
- ✅ 防止重复启动造成的资源浪费
- ✅ 提供友好的用户交互体验

### 2. **多种选择**
- 🔄 **切换到已运行程序** - 恢复并聚焦主窗口
- ❌ **关闭提示** - 继续使用后台运行的程序
- 📊 **显示详细信息** - 查看程序运行状态

### 3. **配置灵活性**
- ⚙️ 可通过托盘菜单开启/关闭此功能
- 🔇 禁用后自动静默处理重复启动
- 💾 配置自动保存和恢复

### 4. **详细信息展示**
- 📈 程序运行时长和内存使用
- 🖥️ 窗口和托盘状态
- 🔧 系统和应用版本信息
- 📋 支持复制信息到剪贴板

## 🧪 测试步骤

### 基础功能测试

#### 1. 启用状态测试
```bash
# 步骤1：启动程序
npm start

# 步骤2：在程序运行时，再次启动
npm start

# 预期结果：显示"MenuorgPrint已在运行"对话框
```

**验证点：**
- ✅ 对话框正常显示
- ✅ 显示当前运行状态信息
- ✅ 三个按钮都可正常点击
- ✅ 应用图标正常显示

#### 2. 切换到已运行程序测试
```bash
# 在对话框中点击"切换到已运行程序"

# 预期结果：
# - 主窗口恢复到前台
# - 窗口闪烁提醒用户
# - 显示"应用窗口已恢复到前台"通知
# - 第二个实例正常退出
```

#### 3. 关闭提示测试
```bash
# 在对话框中点击"关闭提示"

# 预期结果：
# - 对话框关闭
# - 程序继续在后台运行
# - 第二个实例正常退出
```

#### 4. 显示详细信息测试
```bash
# 在对话框中点击"显示详细信息"

# 预期结果：
# - 显示详细运行信息对话框
# - 包含应用状态、配置、系统信息
# - 可复制信息到剪贴板
# - 点击"确定"后返回主对话框
```

### 配置功能测试

#### 5. 通过托盘菜单禁用功能
```bash
# 步骤1：右键托盘图标
# 步骤2：取消选中"💬 程序已运行提示"
# 步骤3：再次启动程序

# 预期结果：
# - 不显示对话框
# - 直接恢复窗口到前台
# - 显示简短通知
# - 控制台输出"程序已运行提示已禁用"
```

#### 6. 重新启用功能
```bash
# 步骤1：右键托盘图标
# 步骤2：选中"💬 程序已运行提示"
# 步骤3：再次启动程序

# 预期结果：
# - 对话框正常显示
# - 所有功能正常工作
```

### 边界情况测试

#### 7. 程序最小化状态测试
```bash
# 步骤1：最小化主窗口
# 步骤2：再次启动程序
# 步骤3：选择"切换到已运行程序"

# 预期结果：
# - 窗口从最小化状态恢复
# - 窗口正常显示并获得焦点
```

#### 8. 程序隐藏状态测试
```bash
# 步骤1：通过托盘菜单隐藏窗口
# 步骤2：再次启动程序
# 步骤3：选择"切换到已运行程序"

# 预期结果：
# - 窗口从隐藏状态显示
# - 窗口正常显示并获得焦点
```

#### 9. 连续启动测试
```bash
# 快速连续启动多个实例

# 预期结果：
# - 只有一个对话框显示
# - 程序处理正常
# - 不会崩溃或卡死
```

## 📊 测试检查表

### 功能完整性
- [ ] 基础对话框显示
- [ ] 切换到已运行程序
- [ ] 关闭提示功能
- [ ] 显示详细信息
- [ ] 详细信息复制功能
- [ ] 托盘菜单配置
- [ ] 配置保存和恢复

### 用户体验
- [ ] 对话框内容清晰易懂
- [ ] 按钮操作符合预期
- [ ] 通知提示及时准确
- [ ] 窗口状态切换流畅
- [ ] 图标显示正常

### 稳定性
- [ ] 连续操作不崩溃
- [ ] 边界情况处理正常
- [ ] 错误恢复机制工作
- [ ] 内存使用合理

### 兼容性
- [ ] Windows 7+ 系统兼容
- [ ] 开发和生产环境一致
- [ ] 不同屏幕分辨率适配

## 🐛 常见问题

### Q: 对话框没有显示？
**A:** 检查以下项目：
1. 功能是否在托盘菜单中启用
2. 控制台是否有错误信息
3. 第一个实例是否正常运行

### Q: 点击"切换到已运行程序"没反应？
**A:** 检查以下项目：
1. 主窗口是否已创建
2. 是否有JavaScript错误
3. 窗口是否被其他程序遮挡

### Q: 详细信息显示不全？
**A:** 这是正常的，可以：
1. 使用"复制到剪贴板"功能
2. 检查控制台的详细日志
3. 调整对话框大小（如果支持）

### Q: 配置不保存？
**A:** 检查以下项目：
1. 配置文件权限
2. 磁盘空间是否充足
3. 控制台错误信息

## 📋 测试报告模板

```
测试日期：[日期]
测试环境：[系统版本] + [Node.js版本] + [Electron版本]
测试人员：[姓名]

基础功能测试：
□ 对话框显示：通过/失败
□ 切换功能：通过/失败
□ 关闭功能：通过/失败
□ 详细信息：通过/失败

配置功能测试：
□ 启用/禁用：通过/失败
□ 配置保存：通过/失败

边界测试：
□ 最小化状态：通过/失败
□ 隐藏状态：通过/失败
□ 连续启动：通过/失败

发现问题：
1. [问题描述]
2. [问题描述]

建议改进：
1. [改进建议]
2. [改进建议]
```

## 🎉 预期效果

完成测试后，应该达到以下效果：

1. **用户友好** - 清晰的提示和选择
2. **功能完整** - 所有预期功能正常工作
3. **配置灵活** - 用户可以自由开启/关闭
4. **稳定可靠** - 各种情况下都能正常工作
5. **体验优秀** - 响应迅速，操作流畅

通过这些测试，确保"程序已运行"功能为用户提供最佳的使用体验！ 